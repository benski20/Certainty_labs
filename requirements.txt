# Certainty Labs — required dependencies
# Install: pip install -r requirements.txt

# API server
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-dotenv>=1.0.0
python-multipart>=0.0.9

# Auth & storage
supabase>=2.0.0

# HTTP client (SDK and tests)
httpx>=0.27.0

# ML / training / inference
torch>=2.2.0
transformers>=4.40.0
accelerate>=0.29.0
numpy>=1.26.0

# Data & config
pydantic>=2.0.0
pyyaml>=6.0
jsonlines>=4.0.0

# LLM sampling (rerank, candidates)
openai>=1.0.0

# Training loop & viz (trainer, optional plots)
tqdm>=4.66.0
matplotlib>=3.8.0
plotly>=5.18.0
